<section>
    <button class="boton"><a href="servicios">¿Necesitas instalar cámaras de seguridad?</a></button>
</section>

<style>
.boton{
  font-size: 24px;
  padding: 1.2em 2em;
  background: linear-gradient(45deg, #1e0c80, #134ebc);
  color: white;
  border: none;
  border-radius: 12px;
  cursor: pointer;
  box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
  animation: pulse 2s infinite;
}

a {
    text-decoration: none;
    color: aliceblue;
}

.boton :hover {
  transform: scale(1.05);
  box-shadow: 0 12px 24px rgba(0, 0, 0, 0.4);
}

@keyframes pulse {
  0% { transform: scale(1); }
  50% { transform: scale(1.07); }
  100% { transform: scale(1); }
}


</style>

